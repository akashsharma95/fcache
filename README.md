Inmemcache
====
simple in-memory cache with an HTTP interface

## Build
* Run docker build
```bash
docker build -t cache .
```

* Run the built image
```bash
docker run --rm -it cache
```

## Usage
```http

```